---
extends: substitution
message: "Use definite article '%s' instead of indefinite 'a/an' for specificity in requirements (INCOSE R5)."
link: https://reqi.io/articles/incose-requirements-quality-42-rule-guide
ignorecase: true
level: warning
swap:
  a system: the system
  a user: the user
  a process: the process
  a component: the component
  a module: the module
  a function: the function
  a service: the service
  a device: the device
  a server: the server
  a client: the client
  a database: the database
  a file: the file
  a message: the message
  a request: the request
  a response: the response
  a connection: the connection
  a session: the session
  a transaction: the transaction
  a record: the record
  a log: the log
  a report: the report
  a notification: the notification
  a command: the command
  a signal: the signal
  a packet: the packet
  a buffer: the buffer
  a queue: the queue
  a timer: the timer
  a counter: the counter
  a sensor: the sensor
  a controller: the controller
  a handler: the handler
  a manager: the manager
  a processor: the processor
  a scheduler: the scheduler
  a dispatcher: the dispatcher
  a listener: the listener
  a subscriber: the subscriber
  a publisher: the publisher
  a consumer: the consumer
  a producer: the producer
  a reader: the reader
  a writer: the writer
  a driver: the driver
  a interface: the interface
  a protocol: the protocol
  a configuration: the configuration
  a parameter: the parameter
  a setting: the setting
  a value: the value
  a threshold: the threshold
  a limit: the limit
  a timeout: the timeout
  a delay: the delay
  a interval: the interval
  a rate: the rate
  a frequency: the frequency
  a mode: the mode
  a state: the state
  a status: the status
  a event: the event
  a error: the error
  a exception: the exception
  a warning: the warning
  a failure: the failure
  a fault: the fault
  an operator: the operator
  an administrator: the administrator
  an application: the application
  an object: the object
  an entity: the entity
  an element: the element
  an attribute: the attribute
  an input: the input
  an output: the output
  an action: the action
  an operation: the operation
  an algorithm: the algorithm
  an API: the API
  an interface: the interface
  an instance: the instance
  an identifier: the identifier
  an event: the event
  an error: the error
  an exception: the exception
  an alert: the alert
  an interrupt: the interrupt
