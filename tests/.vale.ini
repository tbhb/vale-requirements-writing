---
StylesPath = ../styles
MinAlertLevel = warning

# Test files intentionally contain requirements writing issues for testing.
# Only apply RequirementsWriting styles, not external styles like Google/write-good.
[*.md]
BasedOnStyles = RequirementsWriting
